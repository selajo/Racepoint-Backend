package util.client;

import java.util.concurrent.ConcurrentHashMap;

/**
 * Beinhaltet die Binary des Servers.
 */
public class Binaries {
    public static ConcurrentHashMap<Integer, byte[]> clientBinaries;
    public static GameServer gameServer;
}
